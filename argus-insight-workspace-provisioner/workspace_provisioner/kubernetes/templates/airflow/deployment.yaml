apiVersion: apps/v1
kind: Deployment
metadata:
  name: argus-airflow-${WORKSPACE_NAME}-db
  namespace: ${K8S_NAMESPACE}
  labels:
    app.kubernetes.io/name: airflow-db
    app.kubernetes.io/instance: ${WORKSPACE_NAME}
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: argus-insight
    argus-insight/workspace: ${WORKSPACE_NAME}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: airflow-db
      app.kubernetes.io/instance: ${WORKSPACE_NAME}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: airflow-db
        app.kubernetes.io/instance: ${WORKSPACE_NAME}
        app.kubernetes.io/part-of: argus-insight
        argus-insight/workspace: ${WORKSPACE_NAME}
    spec:
      containers:
        - name: postgres
          image: ${AIRFLOW_POSTGRES_IMAGE}
          env:
            - name: POSTGRES_DB
              value: airflow
            - name: POSTGRES_USER
              value: airflow
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: argus-airflow-${WORKSPACE_NAME}
                  key: postgres-password
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "airflow"]
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: db-data
          persistentVolumeClaim:
            claimName: argus-airflow-${WORKSPACE_NAME}-db
---
apiVersion: v1
kind: Service
metadata:
  name: argus-airflow-${WORKSPACE_NAME}-db
  namespace: ${K8S_NAMESPACE}
  labels:
    app.kubernetes.io/name: airflow-db
    app.kubernetes.io/instance: ${WORKSPACE_NAME}
    app.kubernetes.io/part-of: argus-insight
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app.kubernetes.io/name: airflow-db
    app.kubernetes.io/instance: ${WORKSPACE_NAME}
