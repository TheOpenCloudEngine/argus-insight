apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "unity-catalog.fullname" . }}
  labels:
    {{- include "unity-catalog.labels" . | nindent 4 }}
data:
  server.properties: |
    {{- .Values.config.serverProperties | nindent 4 }}
