Unity Catalog has been deployed.

1. Get the application URL:
{{- if eq .Values.service.type "ClusterIP" }}
  kubectl port-forward svc/{{ include "unity-catalog.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Open: http://localhost:{{ .Values.service.port }}/api/2.1/unity-catalog/catalogs
{{- else if eq .Values.service.type "NodePort" }}
  export NODE_PORT=$(kubectl get svc {{ include "unity-catalog.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}")
  export NODE_IP=$(kubectl get nodes -o jsonpath="{.items[0].status.addresses[0].address}")
  Open: http://$NODE_IP:$NODE_PORT/api/2.1/unity-catalog/catalogs
{{- end }}

2. Get the admin token (required when authorization is enabled):
  export TOKEN=$(kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "unity-catalog.fullname" . }} -- cat /home/unitycatalog/etc/conf/token.txt)

  Example API call:
  curl -H "Authorization: Bearer $TOKEN" http://$NODE_IP:$NODE_PORT/api/2.1/unity-catalog/catalogs

3. API Documentation:
  Swagger UI: https://docs.unitycatalog.io/swagger-docs/
  API Base Path: /api/2.1/unity-catalog

{{- if .Values.postgresql.enabled }}

4. PostgreSQL:
  Database: {{ .Values.postgresql.auth.database }}
  Username: {{ .Values.postgresql.auth.username }}
  Internal Service: {{ include "unity-catalog.fullname" . }}-db:5432
{{- end }}
