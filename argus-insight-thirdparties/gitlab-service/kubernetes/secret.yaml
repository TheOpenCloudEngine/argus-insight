apiVersion: v1
kind: Secret
metadata:
  name: gitlab-secrets
  namespace: argus-insight
  labels:
    app.kubernetes.io/name: gitlab
    app.kubernetes.io/part-of: argus-insight
type: Opaque
stringData:
  # Change these values before deploying to production
  initial_root_password: "Argus!nsight2024"
  # GitLab Runner registration token (obtain from GitLab Admin > CI/CD > Runners)
  runner_registration_token: "CHANGE_ME"
  # OpenID Connect client secret for SSO
  oidc_client_secret: "CHANGE_ME"
