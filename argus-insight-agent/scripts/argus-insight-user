#!/usr/bin/env python3
"""Argus Insight Agent - User Management CLI."""

import sys
import os

lib_dir = "/opt/argus-insight-agent"
if os.path.isdir(lib_dir) and lib_dir not in sys.path:
    sys.path.insert(0, lib_dir)

from app.usermgr.cli import main

if __name__ == "__main__":
    main()
