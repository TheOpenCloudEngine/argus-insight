# Argus Insight Server Connection
# This file defines the central server address that this agent reports to.
# The agent sends heartbeat data to the server every 60 seconds.

server.ip=localhost
server.port=8080
