# Catalog Server configuration
server.host=0.0.0.0
server.port=4600

# Logging
log.level=INFO
log.dir=/var/log/catalog-server
log.filename=catalog-server.log

# CORS
cors.origins=*

# Database
db.type=postgresql
db.host=localhost
db.port=5432
db.name=argus_catalog
db.username=argus
db.password=argus
